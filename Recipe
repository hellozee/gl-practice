import "ui"
import "draw"

entity #{
	binary = "g++"
	name = "GLWindow"
	start = "main"
	ldflags = "-lSDL2 -lGLEW -lGL -lSOIL"
	includes = 
	others = "-Wall -Wextra -std=c++11"
}

entity main{
	file = "main.cpp"
	deps = "camera display mesh shader"
}
